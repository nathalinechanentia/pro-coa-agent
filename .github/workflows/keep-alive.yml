name: Keep Streamlit App Awake

on:
  schedule:
    - cron: '0 */10 * * *'   # every 10 hours
  workflow_dispatch:          # allows manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the Streamlit app
        run: curl -I https://nathalinechanentia-pro-coa-agent-app-iej96d.streamlit.app